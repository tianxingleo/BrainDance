# Marker AR 目标图资源说明

- 将打印用目标图放在 `public/targets/` 下，例如 `braindance-card.png`。
- 将对应的 MindAR 描述文件放在同目录，例如 `braindance-card.mind`。
- 当前代码默认读取 `/targets/braindance-card.mind`。
- 仓库暂未附带二进制 `.mind` 产物；接入真实卡片前，请先生成并放入该路径。
