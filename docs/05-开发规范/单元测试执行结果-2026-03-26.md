# 单元测试执行结果

执行日期：2026-03-26  
执行目录：`/home/ltx/projects/BrainDance/ai_engine/3dgs`  
执行环境：`conda run -n gs_linux_backup`  

## 执行说明

- 按 [测试指南](/home/ltx/projects/BrainDance/docs/05-开发规范/测试指南.md) 中 `cd ai_engine/3dgs && python -m pytest tests/ -v` 的入口先执行全量单元测试。
- 当前机器不存在仓库规范要求的 `Braindance` Conda 环境，因此改用 `gs_linux_backup` 环境执行；该环境内具备 `pytest 9.0.1`。
- 全量执行在收集阶段被 `tests/test_upload_fix.py` 阻断，报错为 `ModuleNotFoundError: No module named 'core.pipeline_base'`。
- 为输出每个单元测试及测试结果，后续按单元级测试文件逐个执行并记录结果。
- 下列脚本明显属于集成测试、手工脚本或依赖外部服务，不纳入本次“单元测试”结果表：`tests/test_local_single_image.py`、`tests/test_pipeline_integration.py`、`tests/test_supabase_integration.py`、`tests/test_sharp_simulation.py`、`test_vector_format.py`、`tests/test_pipeline_cases.py` 中除 `test_01_pipeline_creation` 外的其余用例。

## tests/test_upload_fix.py::test_large_upload

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证大文件上传与重试逻辑的测试入口，但当前在模块导入阶段即失败。 |
| 用例目的 | 确认 `BasePipeline.upload_and_record` 能处理大体积 PLY 文件上传。 |
| 前提条件 | 需要存在 `core.pipeline_base` 模块、指定的本地大文件路径、可用的 Supabase 配置。 |
| 特殊的规程说明 | 该用例未进入函数体执行，`pytest` 在收集阶段因导入失败中断。 |
| 用例间的依赖关系 | 无直接依赖，但依赖本地硬编码路径与旧模块结构。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 执行 `conda run -n gs_linux_backup python -m pytest tests/ -v` 收集 `tests/test_upload_fix.py` | 用例被正常收集并执行上传测试 | 收集失败：`ModuleNotFoundError: No module named 'core.pipeline_base'` | 阻断全量 `tests/` 执行；属于历史失效用例。 |

## tests/test_worker_log_filter.py::test_contains_emoji_only_matches_emoji_logs

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `CloudWorker._contains_emoji` 对日志文本的过滤规则。 |
| 用例目的 | 确保仅带 emoji 的关键信息日志会被识别为云端同步日志。 |
| 前提条件 | `src.core.worker.CloudWorker` 可导入；无需数据库或网络。 |
| 特殊的规程说明 | 直接以固定字符串调用静态方法。 |
| 用例间的依赖关系 | 与同文件其他用例无执行顺序依赖。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| `"🚀 启动流水线"`、`"❌ 严重错误: DA3 解算失败"`、`"正在从云端下载资源..."`、`"下载视频..."` | 前两项返回 `True`，后两项返回 `False` | `PASSED`，断言全部成立 | 结果符合 emoji 过滤设计。 |

## tests/test_worker_log_filter.py::test_record_cloud_log_keeps_only_emoji_logs

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_record_cloud_log` 仅保留包含 emoji 的日志，并触发同步。 |
| 用例目的 | 确保云端日志列表不会写入普通过程性日志。 |
| 前提条件 | 使用 `CloudWorker.__new__` 构造对象并注入假的 `_sync_log`。 |
| 特殊的规程说明 | 不走真实 Worker 初始化流程，采用最小对象状态测试。 |
| 用例间的依赖关系 | 与前一个用例逻辑相关，但彼此独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 依次写入 `"正在从云端下载资源..."`、`"🚀 启动流水线"`、`"✅ 任务全部完成"` | 仅保留后两条日志，且 `_sync_log` 被调用两次 | `PASSED`，日志数组长度为 2，同步调用为 `["task_1", "task_1"]` | 过滤和同步逻辑正常。 |

## tests/test_glomap_runner_fallback.py::test_build_cpu_fallback_cmd_only_rewrites_current_command

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_build_cpu_fallback_cmd` 仅重写当前命令中的 GPU 标志。 |
| 用例目的 | 确保 GPU 命令失败回退 CPU 时不会污染原始命令对象和其他状态。 |
| 前提条件 | `GlomapRunner` 类可导入；无需实例化完整 Runner。 |
| 特殊的规程说明 | 直接传入手工构造的 COLMAP 命令数组。 |
| 用例间的依赖关系 | 与同文件其他回退逻辑用例无顺序依赖。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 带 `--FeatureMatching.use_gpu 1` 与 `--FeatureMatching.gpu_index 2` 的命令数组 | 返回新命令；原命令保持 `1/2`，新命令改为 `0/-1` | `PASSED`，断言命令改写与原命令不变同时成立 | CPU 回退命令构造正确。 |

## tests/test_glomap_runner_fallback.py::test_command_uses_gpu_reads_effective_command_state

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_command_uses_gpu` 按命令参数判断是否启用 GPU。 |
| 用例目的 | 确保重试逻辑基于命令当前状态而不是历史状态。 |
| 前提条件 | `GlomapRunner` 类可导入。 |
| 特殊的规程说明 | 直接输入最小命令列表，不依赖外部程序。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| `["colmap", "--FeatureMatching.use_gpu", "1"]` 与 `["colmap", "--FeatureMatching.use_gpu", "0"]` | 前者返回 `True`，后者返回 `False` | `PASSED` | GPU 使用状态判断准确。 |

## tests/test_glomap_runner_fallback.py::test_retry_cpu_does_not_disable_future_gpu_usage

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_run_cmd` 在首次 GPU 执行失败后可回退 CPU，但不会永久关闭 GPU。 |
| 用例目的 | 防止一次失败把 Runner 全局状态错误降级为 CPU-only。 |
| 前提条件 | 通过 `__new__` 构造 Runner，并用 `monkeypatch` 替换 `subprocess.Popen`。 |
| 特殊的规程说明 | 第一次伪造返回码 `1`，第二次返回码 `0`，模拟自动重试。 |
| 用例间的依赖关系 | 依赖同文件的 `make_runner` 辅助函数，无其他依赖。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 启用 GPU 的 COLMAP 命令，`retry_cpu=True` | 发生两次执行：首次 `1/0`，回退后 `0/-1`，且 `runner.colmap_use_gpu` 仍为 `True` | `PASSED` | 回退后不污染未来 GPU 配置。 |

## tests/test_glomap_runner_gpu_index.py::test_colmap_gpu_index_maps_physical_id_to_local_visible_index

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证物理 GPU 编号到 `CUDA_VISIBLE_DEVICES` 局部索引的映射。 |
| 用例目的 | 确保 COLMAP 在受限可见设备集合中拿到正确索引。 |
| 前提条件 | `PipelineConfig`、`GlomapRunner` 可导入。 |
| 特殊的规程说明 | 使用 `patch.dict` 临时设置 `CUDA_VISIBLE_DEVICES=1`。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 环境变量 `CUDA_VISIBLE_DEVICES=1`，配置 `colmap_gpu_index="1"` | Runner 内部索引被映射为 `"0"` | `PASSED` | 单卡可见场景映射正确。 |

## tests/test_glomap_runner_gpu_index.py::test_colmap_gpu_index_preserves_zero_index

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证本地可见索引为 `0` 时不发生错误改写。 |
| 用例目的 | 防止本来正确的 GPU 0 被重复转换。 |
| 前提条件 | `CUDA_VISIBLE_DEVICES=0,1`；Runner 可正常实例化。 |
| 特殊的规程说明 | 通过临时环境变量模拟双卡可见。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 环境变量 `CUDA_VISIBLE_DEVICES=0,1`，配置 `colmap_gpu_index="0"` | Runner 内部索引保持 `"0"` | `PASSED` | 零号索引保留正确。 |

## tests/test_glomap_runner_gpu_index.py::test_colmap_gpu_index_falls_back_when_out_of_visible_range

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证配置索引超出当前可见设备范围时的兜底行为。 |
| 用例目的 | 避免无效 GPU 编号导致 COLMAP 启动失败。 |
| 前提条件 | `CUDA_VISIBLE_DEVICES=1`；Runner 可正常实例化。 |
| 特殊的规程说明 | 用不可见的物理索引 `3` 触发兜底。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 环境变量 `CUDA_VISIBLE_DEVICES=1`，配置 `colmap_gpu_index="3"` | Runner 内部索引回退到 `"0"` | `PASSED` | 越界索引兜底正常。 |

## tests/test_scene_analyzer_single_image.py::test_analyze_single_image_normalizes_string_lists

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证单图分析结果中的字符串列表字段会被标准化。 |
| 用例目的 | 确保模型返回的逗号分隔字符串被转换为结构化列表与默认描述。 |
| 前提条件 | `SceneAnalyzer` 可实例化；通过 `monkeypatch` 注入假的图片 URL 与假客户端。 |
| 特殊的规程说明 | 模拟大模型返回 JSON 字符串，避免真实 API 调用。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 返回内容 `{"score":"88","reason":"清晰","tags":"杯子, 桌子","description":"","objects":"杯子，木桌"}` | `ok=True`，`score=88`，`tags=["杯子","桌子"]`，`objects=["杯子","木桌"]`，自动补全描述 | `PASSED` | 解析与归一化逻辑正确。 |

## tests/test_scene_analyzer_single_image.py::test_analyze_single_image_reports_failure_without_fake_zero_score

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证解析失败时不会伪造 `0` 分，而是返回失败态。 |
| 用例目的 | 避免上层把解析错误误判为低质量图片。 |
| 前提条件 | `SceneAnalyzer` 可实例化；通过 `monkeypatch` 注入非 JSON 返回值。 |
| 特殊的规程说明 | 模拟大模型返回 `not-json`。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 模型返回 `not-json` | `ok=False`，`score is None`，`tags=[]`，`reason` 包含 `Analysis Error` | `PASSED` | 失败分支行为符合预期。 |

## tests/test_scene_analyzer_single_image.py::test_build_image_data_url_keeps_small_png_without_wrong_mime

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 PNG 小图转 Data URL 时保留正确 MIME 类型。 |
| 用例目的 | 防止图片上传给模型时 MIME 被误标成 JPEG。 |
| 前提条件 | 可在临时目录创建 PNG 文件。 |
| 特殊的规程说明 | 仅校验生成 URL 前缀，不依赖真实图片库解析。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 临时文件 `sample.png`，内容以 PNG 魔数开头 | 返回值前缀为 `data:image/png;base64,` | `PASSED` | MIME 判断正确。 |

## tests/test_da3_2dgs_pipeline.py::test_resolve_viewer_port_defaults_to_ephemeral

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 Viewer 端口未配置时回落到临时端口。 |
| 用例目的 | 保证流水线在缺省配置下仍可自动分配可用端口。 |
| 前提条件 | `DA3TwoDGSPipeline` 可实例化。 |
| 特殊的规程说明 | 输入分别为 `None` 和空字符串。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| `_resolve_viewer_port(None)`、`_resolve_viewer_port("")` | 两次都返回 `0` | `PASSED` | 缺省端口策略正确。 |

## tests/test_da3_2dgs_pipeline.py::test_resolve_viewer_port_rejects_occupied_port

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证端口已被占用时抛出错误。 |
| 用例目的 | 避免 Viewer 绑定失败时静默继续执行。 |
| 前提条件 | 能在本机临时监听一个 TCP 端口。 |
| 特殊的规程说明 | 使用 `socket` 临时占位，再调用 `_resolve_viewer_port`。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 已监听的本地随机端口 | 抛出包含 `已被占用` 的 `RuntimeError` | `PASSED` | 端口冲突检测正常。 |

## tests/test_da3_2dgs_pipeline.py::test_run_cmd_streams_newline_logs

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_run_cmd` 可把按行输出的子进程日志流式写回。 |
| 用例目的 | 保证流水线日志对普通换行输出的可观测性。 |
| 前提条件 | 可启动当前 Python 解释器子进程。 |
| 特殊的规程说明 | 子进程执行 `print('hello'); print('world')`。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 子进程标准输出两行 `hello`、`world` | 日志回调中能同时捕获两条内容 | `PASSED` | 换行日志流式转发正常。 |

## tests/test_da3_2dgs_pipeline.py::test_run_cmd_streams_carriage_return_progress

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `_run_cmd` 可正确处理回车覆盖式进度输出。 |
| 用例目的 | 保证带 `\r` 的进度日志不会在回调中丢失。 |
| 前提条件 | 可启动当前 Python 解释器子进程。 |
| 特殊的规程说明 | 子进程循环输出 `progress 0/3` 到 `progress 2/3`，最终打印 `done`。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 带回车进度的 Python 子进程脚本 | 日志中包含 `progress 0/3`、`progress 1/3`、`progress 2/3`、`done` | `PASSED` | 回车进度输出处理正确。 |

## tests/test_sharp_orientation_fix.py::TestSharpOrientationFix::test_rotate_gaussian_ply_x180_updates_positions_and_quaternions

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 PLY 点云绕 X 轴旋转 180 度后，位置与四元数同步更新。 |
| 用例目的 | 确保 SHARP 输出模型纠偏时旋转结果数学上正确。 |
| 前提条件 | `plyfile` 可用，且 `rotate_gaussian_ply_x180` 可导入。 |
| 特殊的规程说明 | 在临时目录写入含两条顶点记录的二进制 PLY，再原地旋转后读取断言。 |
| 用例间的依赖关系 | 独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| 两个带 `x/y/z` 与 `rot_0~rot_3` 的高斯点记录 | 位置应变为 `x=[1,-4]`、`y=[-2,-5]`、`z=[-3,6]`，且 `rot_1=[1.0,0.5]` 等四元数结果匹配预期 | `FAILED`，位置断言通过，但 `rot_1` 实际为 `[-0.0,-0.5]`，与期望 `[1.0,0.5]` 不一致 | 暴露四元数旋转实现或测试期望存在偏差，需要排查。 |

## tests/test_pipeline_cases.py::TestSAM3DPipeline::test_01_pipeline_creation

| 项目 | 内容 |
|---|---|
| 测试单元描述 | 验证 `PipelineFactory` 能创建 `SingleImageSAM3DPipeline` 实例。 |
| 用例目的 | 确保工厂注册与基础上下文字段传递正常。 |
| 前提条件 | `PipelineFactory`、`SingleImageSAM3DPipeline`、`PipelineConfig` 可导入。 |
| 特殊的规程说明 | 仅验证实例化，不执行实际 SAM3D 推理。 |
| 用例间的依赖关系 | 其余 `test_pipeline_cases.py` 用例依赖同一工厂与上下文模式，但本用例本身独立。 |

| 输入 | 期望输出 | 实际输出 | 备注 |
|---|---|---|---|
| `task_id=test_task_001`、`scene_id=test_scene_001` 的模拟上下文，调用 `PipelineFactory.get_pipeline("single_image_sam3d", context)` | 返回 `SingleImageSAM3DPipeline` 实例，且 `task_id`、`scene_id` 被正确写入实例 | `PASSED` | `pytest` 额外给出 `PytestReturnNotNoneWarning`，因为测试函数返回了 `True`。 |

## 结果汇总

| 统计项 | 结果 |
|---|---|
| 全量入口执行 | 失败，收集阶段被 `tests/test_upload_fix.py` 阻断 |
| 逐项执行的单元测试数 | 18 |
| 通过 | 16 |
| 失败 | 1 |
| 收集错误 | 1 |
| 主要失败点 | `test_sharp_orientation_fix.py` 四元数断言失败；`test_upload_fix.py` 模块导入路径失效 |
