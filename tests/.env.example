# BrainDance 搜索 API 测试配置
# 复制此文件为 .env.local 并填入实际值

# 方式 1: 直接指定完整的 API URL（优先级最高）
SEARCH_API_URL=http://127.0.0.1:54321/functions/v1/search-models

# 方式 2: 或者使用 SUPABASE_URL（会自动构建完整 URL）
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_URL=https://your-project.supabase.co

# 注意：测试脚本会自动按优先级读取配置：
# 1. 命令行参数
# 2. SEARCH_API_URL 环境变量
# 3. SUPABASE_URL 环境变量
# 4. 默认本地地址
#
# 测试脚本还会自动加载以下 .env 文件（按优先级）：
# - tests/.env.local
# - ai_engine/3dgs/.env
# - tests/.env
