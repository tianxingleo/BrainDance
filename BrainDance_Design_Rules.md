# BrainDance 个性化设计规则与 AGENT 准则

本文件作为 BrainDance 项目的专属设计与代码风格指南，用于彻底压制默认的“AI 味”科技皮，建立真实、克制、物理感强的“空间档案室/扫描工作台”风格体系。

## 1. 核心气质定位
不要把目标理解成“抄一个炫酷动画”，而是建立一套 **Motion Language (动效语言)**。
*   **档案系统**：克制、安静、有层级。
*   **地图工具**：信息准确，交互可信。
*   **摄影接触表 / 扫描工作台**：有现场感，不漂。
*   **实验记录界面**：真实、有过程，不装饰。

## 2. 界面配色 (严禁使用默认 AI 紫/蓝)
*   **主色调**：纸白 (略暖) / 石灰 / 墨黑 / 钝蓝灰 / 旧银。
*   **强调色**：钝蓝灰、旧地图蓝 (严禁荧光发光体)。
*   **提醒/异常状态**：暗红棕。
*   **过程状态色**：褪色橄榄绿。
*   *禁止事项*：禁止紫/蓝紫渐变，禁止大面积玻璃拟态默认卡片。

## 3. UI 布局与元素
*   **不对称布局**：摒弃对称的三分卡片，采用侧重式布局（任务占大块、检索占小块等）。
*   **大圆角与阴影**：卡片阴影极度克制，取消 Android 默认重影，采用微弱的 `rgba(X,X,X, 0.05)` 。
*   **不要 Hero 强引导**：不使用宏大宣发式标题。改为记录式数据，例如“今天新采集2个空间”、“最近常搜：钥匙”。

## 4. 文案规范
废除诸如“智能重构”、“开启未来”、“唤醒空间”等悬浮词，只允许以下3类真实短句：
1.  **状态句**：这个场景还在重建 / 昨晚的扫描已完成。
2.  **动作句**：找刚才放的钥匙 / 对比这周摆放变化。
3.  **现场句**：这个角落不够稳 / 右侧有死角。

## 5. 动效系统 (Motion Tokens)
*   **轻交互**：120-180ms
*   **卡片浮现**：220-280ms
*   **页面切换**：320-420ms
*   **曲线**：进入 `easeOutCubic`，离开 `easeInCubic`，跟手 `easeOutQuart`
*   **克制动画**：不允许全局大面积悬浮背景。扫描页必须像 HUD 工作台，通过实时的环形进度、角点捕捉呼吸灯等小动画展示“仪器工作”的真实感。

## 6. 技术实现栈 (Flutter)
优先采用稳定的原生 `ImplicitlyAnimatedWidget`：
*   卡片状态、导航岛：`AnimatedContainer`、`AnimatedSlide`、`AnimatedOpacity`。
*   特定组件替换（如任务卡片、顶部层级）：`AnimatedSwitcher`。
*   场景切换：利用 `Hero` 或者 `animations` 包的 `SharedAxisTransition` 提供一致的过渡。
