# BrainDance 3DGS 本机覆盖配置
# 说明：
# 1. 这里只放当前机器需要覆盖的非敏感参数。
# 2. 优先覆盖显存守护的预留量，避免默认值过低。

[vram_guard]
# L20 44GB 显存下，把守护进程目标预留从 24GB 提高到 32GB。
# 仍保留 default.toml 里的 8GB 最小可用显存，给主流程留出启动和突发空间。
reserve_gb = 32.0
